# Core runtime
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
scipy>=1.11.0

# Contextual transformer training/inference
# Keep the lower torch bound broad so Colab can reuse its preinstalled GPU build.
torch>=2.2.0
# The current DeBERTa artifact was exported with 5.0.0; 5.7.0 collapses logits to ~0.5.
transformers==5.0.0
safetensors>=0.4.3
sentencepiece>=0.2.0

# Dataset download/export
datasets>=2.18.0
kagglehub>=0.3.0

# Web app and serving
flask>=3.0.0
flask-cors>=4.0.0
joblib>=1.3.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# Tests
pytest>=8.0.0
pytest-cov>=5.0.0
